
<div class="flex items-center gap-2">
    <img src="{{asset('/DB_LOGO__.png')}}" alt="Logo" class="h-12">
    {{-- @if (Route::currentRouteName() !== 'home')
        <span class="text-xl font-bold italic">{{ isset ($title) ? $title . ' - ' : '' }}</span>
    @endif --}}
</div>
